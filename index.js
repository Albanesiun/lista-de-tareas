var button
window.addEventListener("load", function index () {
    button = document.getElementById("button")
})

function main(e) {
    e.preventDefault()
    var task = document.getElementById("añadir")
    console.log(añadir)
    // alert("")
}

if (button) button.addEventListener("click", main)